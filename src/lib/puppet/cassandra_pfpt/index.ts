import { metadata } from './metadata';
import { manifests } from './manifests';
import { templates } from './templates';
import { scripts } from './scripts';
import { files } from './files';

export const cassandra_pfpt = {
  'metadata.json': metadata,
  manifests,
  templates,
  scripts,
  files,
};
