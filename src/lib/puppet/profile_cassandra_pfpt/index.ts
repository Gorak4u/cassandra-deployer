import { metadata } from './metadata';
import { manifests } from './manifests';
import { readme } from './readme';

export const profile_cassandra_pfpt = {
  'metadata.json': metadata,
  'README.md': readme,
  manifests,
};
