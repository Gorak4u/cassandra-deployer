# This file is managed by Puppet.
# cassandra-env.sh

# The directory where Cassandra's logs are stored.
CASSANDRA_LOG_DIR=/var/log/cassandra

# The directory where Cassandra's data is stored.
CASSANDRA_DATA_DIR=/var/lib/cassandra/data

# The directory where Cassandra's commit logs are stored.
CASSANDRA_COMMIT_LOG_DIR=/var/lib/cassandra/commitlog

# The directory where Cassandra's caches are stored.
CASSANDRA_CACHE_DIR=/var/lib/cassandra/saved_caches
